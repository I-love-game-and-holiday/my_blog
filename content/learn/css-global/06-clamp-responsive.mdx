---
title: clamp() でレスポンシブ対応
order: 6
description: メディアクエリを使わずに、画面幅に応じてフォントサイズや余白を滑らかに変化させる clamp() を学びます。
---

## レスポンシブ対応

> PCでは大きく表示したいけれど、スマホでは画面幅に収まるよう小さくしたい。

こういった目的のために「画面幅に応じてサイズを変える」ことを**レスポンシブ対応**と呼びます。

## レスポンシブの2つのアプローチ

CSSでレスポンシブ対応を実現する主要な方法は**2つ**あります。

<ContentTable
  headers={["方法", "変化の仕方", "得意なこと"]}
  rows={[
    ["メディアクエリ（@media）", "ブレークポイントで切り替わる", "レイアウトの構造変更（表示/非表示、縦並び↔横並び）"],
    ["clamp()", "画面幅に応じて滑らかに変化する", "フォントサイズ、余白、幅などのサイズ値"],
  ]}
/>

どちらが優れているかではなく、**得意な場面が違います**。このレッスンでは両方の特徴を理解した上で、`clamp()` の書き方を学びます。

<LearnMore>
この2つ以外に、**コンテナクエリ**（`@container`）という方法もあります。メディアクエリが「画面全体の幅」で切り替えるのに対し、コンテナクエリは「親要素の幅」で切り替えます。同じコンポーネントをサイドバーにもメインカラムにも配置できる場面で便利です。2023年に主要ブラウザすべてで対応し、実務でも採用が進んでいます。
</LearnMore>

## メディアクエリ — ブレークポイントで「切り替える」

**メディアクエリ**（`@media`）は、画面幅がある値を超えたらスタイルを切り替える方法です。

```css title="style.css"
h1 {
  font-size: 24px;
}

@media (min-width: 768px) {
  h1 {
    font-size: 30px;
  }
}
```

これは「画面幅が768px以上になったら、`h1` のフォントサイズを30pxに変える」という意味です。

この方法でも目的は達成できますが、1つ問題があります。

<ContentTable
  headers={["画面幅", "フォントサイズ", "変化"]}
  rows={[
    ["767px", "24px", ""],
    ["768px", "30px", "← ここで一気に6px大きくなる"],
    ["1920px", "30px", ""],
  ]}
/>

768pxを境に、フォントサイズが**一気に切り替わります**。画面幅をゆっくり広げていくと、ある瞬間に「カクッ」と大きくなるのが分かります。

{/* TODO: リサイズ可能なプレビューコンポーネントでメディアクエリの「カクッ」を体験できるデモを追加 */}

メディアクエリはレイアウトの**構造を切り替える**場面で力を発揮します。「スマホでは縦並び、PCでは横並び」「スマホではメニューを隠す」といった、**表示するかしないかの二択**に向いています。

一方で、フォントサイズや余白のように**少しずつ変えたい**値には、次に紹介する `clamp()` が適しています。

## clamp() — 画面幅に応じて「滑らかに」変化させる

`clamp()` を使うと、画面幅に応じてフォントサイズが**滑らかに**変化します。

```css title="style.css"
h1 {
  font-size: clamp(1.5rem, 1.25rem + 1vw, 1.875rem);
}
```

メディアクエリのような「境界線」はありません。画面幅に連動して、最小値と最大値の間を滑らかに移動します。

<ContentTable
  headers={["画面幅", "フォントサイズ", "変化"]}
  rows={[
    ["320px", "24px（最小値）", ""],
    ["500px", "約25.6px", "↕ 滑らかに変化"],
    ["768px", "約27.4px", "↕ 滑らかに変化"],
    ["1024px", "約29px", "↕ 滑らかに変化"],
    ["1280px以上", "30px（最大値）", ""],
  ]}
/>

## clamp() の3つの引数

`clamp()` には3つの値を渡します。

```css title="構文"
clamp(最小値, 推奨値, 最大値)
```

<ContentTable
  headers={["引数", "役割", "例"]}
  rows={[
    ["最小値", "これより小さくならない", "1.5rem（24px）"],
    ["推奨値", "画面幅に応じて計算される値", "1.25rem + 1vw"],
    ["最大値", "これより大きくならない", "1.875rem（30px）"],
  ]}
/>

構成の概要の実際のコードを分解してみましょう。

```css title="style.css"
h1 {
  font-size: clamp(1.5rem, 1.25rem + 1vw, 1.875rem);
}
```

- **`1.5rem`**（24px）: スマホでもこれ以上は小さくならない
- **`1.25rem + 1vw`**: ビューポート幅に応じて計算される値。`1vw` は画面幅の1%
- **`1.875rem`**（30px）: PCでもこれ以上は大きくならない

推奨値の `1vw` がポイントです。画面幅が広がると `1vw` の値も大きくなるため、フォントサイズが連動して変化します。

<LearnMore>
`rem` はルート要素（`html`）のフォントサイズを基準とした単位です。ブラウザのデフォルトでは `1rem = 16px` です。

<ContentTable
  headers={["値", "計算", "結果"]}
  rows={[
    ["1.5rem", "16px × 1.5", "24px"],
    ["1.875rem", "16px × 1.875", "30px"],
  ]}
/>

`px` ではなく `rem` を使う理由は、ユーザーがブラウザの文字サイズ設定を変更したときに、サイト全体のフォントサイズが連動して変わるようにするためです。アクセシビリティの観点から、フォントサイズには `rem` を使うのが推奨されています。
</LearnMore>

## フォントサイズ以外にも使える

`clamp()` はフォントサイズ専用の機能ではありません。**サイズを表すあらゆるプロパティ**に使えます。

```css title="style.css"
/* 余白を滑らかに変化させる */
.section {
  padding: clamp(2rem, 1.5rem + 2vw, 4rem);
}

/* コンテンツ幅を滑らかに変化させる */
.container {
  max-width: clamp(20rem, 90%, 56rem);
}
```

<CodePlayground
  height={250}
  title="clamp() でパディングを滑らかに変化させる"
  html={`
    <div class="section">
      <h1>見出しテキスト</h1>
      <p>このセクションのパディング（内側の余白）は、画面幅に応じて滑らかに変化します。</p>
    </div>
  `}
  css={`
    .section {
      padding: clamp(1rem, 0.5rem + 2vw, 3rem);
      background-color: #f0f9ff;
      border: 1px solid #bfdbfe;
    }
    h1 {
      font-size: clamp(1.25rem, 1rem + 1vw, 1.75rem);
      margin-bottom: 8px;
    }
    p {
      line-height: 1.6;
    }
  `}
/>

<Callout type="info">
冒頭で見た2つのアプローチを思い出しましょう。構造の切り替え（表示/非表示、縦/横）は**メディアクエリ**、サイズ値の滑らかな変化は **`clamp()`**。それぞれの得意分野を使い分けるのがポイントです。
</Callout>

<LearnMore>
推奨値の計算式（`1.25rem + 1vw` のような部分）を自分で考えるのは大変です。最小値と最大値を入力すると、推奨値を自動計算してくれるツールがあります。

- [Utopia](https://utopia.fyi/type/calculator/) — フォントサイズや余白のclamp()を自動生成
- [clamp() Calculator](https://www.marcbacon.com/tools/clamp-calculator/) — シンプルなclamp()計算機

最小値・最大値・対応する画面幅を入力するだけで、推奨値を含む `clamp()` 式が生成されます。
</LearnMore>

## 構成の概要を振り返ろう

これで構成の概要の5項目すべてを理解できました。

<CollapsibleCode title="style.css（構成の概要）" defaultOpen={false}>
```css
/* 1. CSSリセット */
*, *::before, *::after { box-sizing: border-box; margin: 0; }

/* 2. ベーススタイル */
body { font-family: system-ui, sans-serif; line-height: 1.6; }

/* 3. デザイントークン（CSS変数） */
:root { --background: #ffffff; --text: #171717; }

/* 4. ダークモードの色定義 */
.dark { --background: #171717; --text: #ffffff; }

/* 5. レスポンシブなサイズ定義 */
h1 { font-size: clamp(1.5rem, 1.25rem + 1vw, 1.875rem); }
```
</CollapsibleCode>

```css title="style.css（すべて理解できた！）"
/* 1. CSSリセット ✅ */
*, *::before, *::after { box-sizing: border-box; margin: 0; }

/* 2. ベーススタイル ✅ */
body { font-family: system-ui, sans-serif; line-height: 1.6; }

/* 3. デザイントークン（CSS変数） ✅ */
:root { --background: #ffffff; --text: #171717; }

/* 4. ダークモードの色定義 ✅ */
.dark { --background: #171717; --text: #ffffff; }

/* 5. レスポンシブなサイズ定義 ✅ */
h1 { font-size: clamp(1.5rem, 1.25rem + 1vw, 1.875rem); }
```

## 次のステップ

01で見せたグローバルCSSの構成をすべて読み解けるようになりました。次のレッスンでは、CSSファイルが増えてきたときに読み込み順を整理する **@layer** を学びます。
