---
title: グローバルCSSとは何か
order: 1
description: なぜ「アプリ全体に適用されるCSS」が必要なのか。コンポーネントCSSとの違いと、グローバルCSSの役割を理解します。
---

## CSSファイルにはいくつか種類がある

Webアプリを作っていると、CSSの書き方にはいくつか種類があることに気づきます。

<ContentTable
  headers={["種類", "スコープ", "例"]}
  rows={[
    ["グローバルCSS", "サイト全体", "globals.css, index.css, style.css"],
    ["CSSモジュール", "1つのコンポーネント", "button.module.css"],
    ["CSS-in-JS", "1つのコンポーネント", "styled-components, emotion"],
    ["ユーティリティCSS", "1つの要素", "Tailwind CSS のクラス"],
  ]}
/>

<Callout type="info">
上の表が難しいと感じた人は **『cssにはアプリ全体に適用されるものと、小さい部品に適用されるものがあるんだな～』**
と思ってください。

</Callout>
## グローバルCSSの役割

グローバルCSSは「**どのページを開いても適用されるCSS**」です。

具体的例を見てみましょう。

- サイト全体の**色**（背景色、文字色、ボーダー色など）
- サイト全体の**デザイントークン**（余白やフォントサイズのルール）
- **ダークモード**の色定義
- ベースとなるタイポグラフィ（フォント、行間、見出しサイズ）
- CSSリセット（ブラウザのデフォルトスタイルをそろえる）

<ContentImage
  src="/learn/css-global/dark_mode.jpg"
  alt="グローバルCSSの例"
  caption=""
  width={500}
  height={450}
/>

## どのフレームワークにもこの概念がある

「グローバルCSS」はフレームワーク固有の機能ではなく、**Web開発に共通する概念**です。ファイル名や読み込み方が違うだけで、どの環境にも存在します。

<ContentTable
  headers={["環境", "ファイル名の例", "読み込み方"]}
  rows={[
    ["素のHTML", "style.css", "<link> タグで読み込み"],
    ["Next.js", "globals.css", "layout.tsx で import"],
    ["Vite (React)", "index.css", "main.tsx で import"],
    ["Nuxt", "main.css", "nuxt.config.ts で設定"],
    ["SvelteKit", "app.css", "+layout.svelte で import"],
  ]}
/>

名前は違いますが、役割は同じです。**アプリ全体に影響するスタイルを1箇所で定義する**というのが共通の考え方です。

## 素のHTMLでの読み込み方

もっともシンプルな例を見てみましょう。素のHTMLでは、`<link>` タグで1回読み込むだけです。

```html title="index.html"
<!DOCTYPE html>
<html>
<head>
  <link rel="stylesheet" href="style.css" />
</head>
<body>
  <h1>Hello</h1>
</body>
</html>
```

これだけで、サイト内のすべてのページに `style.css` のスタイルが適用されます。フレームワークを使う場合も、やっていることの本質はこれと同じです。

## グローバルCSSの中身を覗いてみよう

典型的なグローバルCSSは、大きく分けて以下のような構成になっています。

```css title="style.css（構成の概要）"
/* 1. CSSリセット */
*, *::before, *::after { box-sizing: border-box; margin: 0; }

/* 2. デザイントークン（CSS変数） */
:root { --background: #ffffff; --text: #171717; }

/* 3. ダークモードの色定義 */
.dark { --background: #171717; --text: #ffffff; }

/* 4. ベーススタイル */
body { background-color: var(--background); color: var(--text); }

/* 5. タイポグラフィ */
h1, h2, h3 { line-height: 1.3; }

/* 6. レスポンシブなサイズ定義 */
h1 { font-size: clamp(1.5rem, 1.25rem + 1vw, 1.875rem); }
```

最初から全部理解する必要はありません。このコースでは、1つずつ順番に「何をしているのか」「なぜこう書くのか」を解説していきます。

## コンポーネントCSS との違い

「全部グローバルCSSに書けばいいのでは？」と思うかもしれません。しかし、グローバルCSSにすべてを書くと問題が起きます。

- ファイルが巨大になり、どこに何があるか分からなくなる
- クラス名が衝突する（`.title` が別の場所で上書きされる）
- 変更の影響範囲が分からない（1行直したら別のページが崩れる）

だから、**サイト全体に関わるものだけ**グローバルCSSに書き、コンポーネント固有のスタイルは別の方法（CSSモジュールやユーティリティCSS）で管理します。

<ContentTable
  headers={["書く場所", "内容"]}
  rows={[
    ["グローバルCSS", "色定義、ダークモード、フォント設定、リセット、デザイントークン"],
    ["コンポーネント側", "ボタンの大きさ、カードのレイアウト、アニメーション"],
  ]}
/>

## 次のステップ

グローバルCSSの中身を読むために、まず知っておかないといけないのが **CSS の優先順位（カスケードと詳細度）** です。次のレッスンで学びましょう。
