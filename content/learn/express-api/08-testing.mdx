---
title: テストを書く
order: 8
description: Vitestでバリデーション関数とAPIエンドポイントのテストを書きます。
---

## このレッスンで学ぶこと

**テスト** を書いて、コードが正しく動くことを自動で検証する方法を学びます。

今までは curl で手動テストしていましたが、機能が増えるにつれて毎回全てを手動で確認するのは現実的ではありません。テストを書いておけば、コマンド1つで全ての検証を自動実行できます。

## なぜテストを書くのか

<ContentTable
  headers={["メリット", "説明"]}
  rows={[
    ["バグの早期発見", "コードを変更したときに、既存の機能が壊れていないかすぐに分かる"],
    ["安心してコードを変更できる", "テストが通れば、変更が既存機能に影響していないことが保証される"],
    ["仕様書の代わりになる", "テストコードを読めば「何が正しい動作か」が分かる"],
  ]}
/>

<Callout type="info">
テストは「書くのが面倒」と思われがちですが、手動テストの繰り返しの方が長期的にはずっと面倒です。

特にコース3（リファクタリング）では、テストがあるからこそ安心してコードを変更できるようになります。
</Callout>

## Step 1: Vitestをインストールする

テストフレームワークとして **Vitest** を使います。

```bash
npm install -D vitest
```

<Callout type="info">
**Vitest** は高速で使いやすいテストフレームワークです。設定がほぼ不要で、TypeScriptもそのまま使えます。
</Callout>

## Step 2: テストスクリプトを追加する

`package.json` の `scripts` にテスト用のコマンドを追加します。

```json title="package.json"
"scripts": {
  "dev": "tsx watch src/index.ts",
  "build": "tsc",
  "start": "node dist/index.js",
  "test": "vitest run",
  "test:watch": "vitest"
}
```

<ContentTable
  headers={["コマンド", "説明"]}
  rows={[
    ["npm test", "テストを1回実行する"],
    ["npm run test:watch", "ファイル変更時に自動でテストを再実行する"],
  ]}
/>

## Step 3: 最初のテストを書く

バリデーション関数のテストから始めましょう。

`src/validation.test.ts` を作成してください。

```typescript title="src/validation.test.ts"
import { describe, it, expect } from 'vitest';
import { isValidDate, isValidTime, isClockOutAfterClockIn } from './validation.js';

describe('isValidDate', () => {
  it('正しい日付形式を受け付ける', () => {
    expect(isValidDate('2026-01-28')).toBe(true);
    expect(isValidDate('2026-12-31')).toBe(true);
  });

  it('不正な形式を拒否する', () => {
    expect(isValidDate('abc')).toBe(false);
    expect(isValidDate('2026/01/28')).toBe(false);
    expect(isValidDate('28-01-2026')).toBe(false);
  });

  it('存在しない日付を拒否する', () => {
    expect(isValidDate('2026-13-01')).toBe(false);
    expect(isValidDate('2026-02-30')).toBe(false);
  });
});

describe('isValidTime', () => {
  it('正しい時刻形式を受け付ける', () => {
    expect(isValidTime('09:00')).toBe(true);
    expect(isValidTime('23:59')).toBe(true);
    expect(isValidTime('00:00')).toBe(true);
  });

  it('不正な形式を拒否する', () => {
    expect(isValidTime('abc')).toBe(false);
    expect(isValidTime('9:00')).toBe(false);
    expect(isValidTime('25:00')).toBe(false);
    expect(isValidTime('12:60')).toBe(false);
  });
});

describe('isClockOutAfterClockIn', () => {
  it('退勤が出勤より後ならtrueを返す', () => {
    expect(isClockOutAfterClockIn('09:00', '18:00')).toBe(true);
  });

  it('退勤が出勤より前ならfalseを返す', () => {
    expect(isClockOutAfterClockIn('09:00', '08:00')).toBe(false);
  });

  it('同じ時刻ならfalseを返す', () => {
    expect(isClockOutAfterClockIn('09:00', '09:00')).toBe(false);
  });
});
```

### テストの構造

```typescript
describe('isValidDate', () => {    // テストグループの名前
  it('正しい日付形式を受け付ける', () => {  // 個別のテストケース
    expect(isValidDate('2026-01-28')).toBe(true);  // 期待する結果
  });
});
```

<ContentTable
  headers={["関数", "役割"]}
  rows={[
    ["describe()", "テストをグループにまとめる"],
    ["it()", "1つのテストケースを定義する"],
    ["expect()", "テスト対象の値を指定する"],
    [".toBe()", "期待する結果と一致するか検証する"],
  ]}
/>

<Callout type="info">
`it()` の第1引数には「何をテストしているか」を日本語で書くと読みやすくなります。テストが失敗したときに、どのケースが失敗したのか一目で分かります。
</Callout>

## Step 4: テストを実行する

```bash
npm test
```

全てのテストが通れば、以下のような出力が表示されます。

```
 ✓ src/validation.test.ts (8)
   ✓ isValidDate (3)
     ✓ 正しい日付形式を受け付ける
     ✓ 不正な形式を拒否する
     ✓ 存在しない日付を拒否する
   ✓ isValidTime (2)
     ✓ 正しい時刻形式を受け付ける
     ✓ 不正な形式を拒否する
   ✓ isClockOutAfterClockIn (3)
     ✓ 退勤が出勤より後ならtrueを返す
     ✓ 退勤が出勤より前ならfalseを返す
     ✓ 同じ時刻ならfalseを返す

 Test Files  1 passed (1)
      Tests  8 passed (8)
```

全てグリーン（合格）です。

## Step 5: テストを失敗させてみる

テストの価値を体感するために、わざとバリデーション関数を壊してみましょう。

`src/validation.ts` の `isValidTime` を以下のように変更します。

```typescript title="src/validation.ts"
export function isValidTime(time: string): boolean {
  return true; // 常にtrueを返す（壊れた状態）
}
```

テストを実行すると：

```bash
npm test
```

```
 ✗ src/validation.test.ts (8)
   ✓ isValidDate (3)
   ✗ isValidTime (2)
     ✓ 正しい時刻形式を受け付ける
     ✗ 不正な形式を拒否する
```

テストが失敗し、どこが壊れたか一目で分かります。

確認したら、`isValidTime` を元に戻してください。

## Step 6: チャレンジ - データ操作のテストを書く

余裕がある方は、`src/data.ts` のテストも書いてみましょう。

<CollapsibleCode title="ヒント: どんなテストを書くか" defaultOpen={false}>
```
- getRecords() が配列を返すこと
- getRecordById() で存在するIDを指定すると記録が返ること
- getRecordById() で存在しないIDを指定すると undefined が返ること
- addRecord() で記録を追加すると、getRecords() の結果に含まれること
- deleteRecord() で存在するIDを指定すると true が返ること
- deleteRecord() で存在しないIDを指定すると false が返ること
```
</CollapsibleCode>

<CollapsibleCode title="答え: src/data.test.ts" defaultOpen={false}>
```typescript
import { describe, it, expect } from 'vitest';
import { getRecords, getRecordById, addRecord, deleteRecord } from './data.js';
import { AttendanceRecord } from './types.js';

describe('getRecords', () => {
  it('配列を返す', () => {
    const records = getRecords();
    expect(Array.isArray(records)).toBe(true);
  });

  it('初期データが含まれている', () => {
    const records = getRecords();
    expect(records.length).toBeGreaterThan(0);
  });
});

describe('getRecordById', () => {
  it('存在するIDで記録を取得できる', () => {
    const record = getRecordById('1');
    expect(record).toBeDefined();
    expect(record?.id).toBe('1');
  });

  it('存在しないIDでundefinedを返す', () => {
    const record = getRecordById('999');
    expect(record).toBeUndefined();
  });
});

describe('addRecord', () => {
  it('記録を追加できる', () => {
    const newRecord: AttendanceRecord = {
      id: '100',
      employeeName: 'テストユーザー',
      date: '2026-01-28',
      clockIn: '09:00',
      clockOut: null,
    };

    addRecord(newRecord);

    const found = getRecordById('100');
    expect(found).toBeDefined();
    expect(found?.employeeName).toBe('テストユーザー');
  });
});

describe('deleteRecord', () => {
  it('存在するIDを削除するとtrueを返す', () => {
    const result = deleteRecord('100');
    expect(result).toBe(true);
  });

  it('存在しないIDを削除するとfalseを返す', () => {
    const result = deleteRecord('999');
    expect(result).toBe(false);
  });
});
```
</CollapsibleCode>

## 現在のフォルダ構造

<FileTree>
  <FileTreeFolder name="attendance-api" defaultOpen>
    <FileTreeFolder name="node_modules" />
    <FileTreeFolder name="src" defaultOpen>
      <FileTreeFile name="data.ts" />
      <FileTreeFile name="data.test.ts" />
      <FileTreeFile name="index.ts" />
      <FileTreeFile name="types.ts" />
      <FileTreeFile name="validation.ts" />
      <FileTreeFile name="validation.test.ts" active />
    </FileTreeFolder>
    <FileTreeFile name="package.json" />
    <FileTreeFile name="package-lock.json" />
    <FileTreeFile name="tsconfig.json" />
  </FileTreeFolder>
</FileTree>

## 次のステップ

テストを書いて、コードの品質を自動で検証する方法を学びました。

次のレッスンでは、このコースで学んだことを振り返り、次のステップへの道筋を確認します。
