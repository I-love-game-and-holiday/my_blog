---
title: "SQLって画像保存できるんですか!?"
date: "2026-12-06"
description: "BLOBカラムに画像を保存してみます"
tag: "BLOB, MySQL"
---

import { FileTree } from "nextra/components";
import { CodeBlock } from "@/components/CodeBox";

# SQL って画像保存できるんですか!?

> [!NOTE]
>
> この記事はハンズオン形式で blob に画像保存ができることを確認します。

非構造化データ(画像や動画など)は AWS S3 のようなオブジェクトストレージが担うイメージが強かったのですが、
SQL でも保存できると聞いたので試してみます。

### BLOB(ブロブ)

- **バイナリデータを格納するためのデータ型**
- 文字コードに依存しない生のデータをそのまま保存でき、サイズによって TINYBLOB、BLOB、MEDIUMBLOB、LONGBLOB の 4 種類がある

### 環境構築＆スキーマ挿入

#### docker-compose.yml

```yml filename="docker-compose.yml"
version: "3.8"

services:
mysql-server:
image: mysql:8.0
container_name: mysql-server
environment:
MYSQL_ROOT_PASSWORD: rootpassword
MYSQL_DATABASE: testdb
TZ: Asia/Tokyo
ports: - "3306:3306"
volumes: - mysql-data:/var/lib/mysql - ./app:/app
working_dir: /app
command: --default-authentication-plugin=mysql_native_password

volumes:
mysql-data:

```

#### ディレクトリ構成

<FileTree>
  <FileTree.Folder name="app" defaultOpen>
    <FileTree.File name="sample.png" />
  </FileTree.Folder>{" "}
  <FileTree.File name="docker-compose.yml" />
</FileTree>
sample.pngは適当な画像を置いてください

## まとめ

Next.js と nextra-theme-blog を使えば、開発者向けのブログを簡単に構築できます。ぜひ試してみてください！
